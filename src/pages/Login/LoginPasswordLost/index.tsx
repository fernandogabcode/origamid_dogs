import React, { ReactElement } from 'react';

type Props = {};

const LoginPasswordLost: React.FC = (props: Props): ReactElement => {
	return <div>LoginPasswordLost</div>;
};

export default LoginPasswordLost;
