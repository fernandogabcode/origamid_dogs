import React, { ReactElement } from 'react';

type Props = {};

const LoginPasswordReset: React.FC = (props: Props): ReactElement => {
	return <div>LoginPasswordReset</div>;
};

export default LoginPasswordReset;
