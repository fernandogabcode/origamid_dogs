import React, { ReactElement } from 'react';

type Props = {};

const LoginCreate: React.FC = (props: Props): ReactElement => {
	return (
		<section>
			<h2>Login Create</h2>
		</section>
	);
};

export default LoginCreate;
